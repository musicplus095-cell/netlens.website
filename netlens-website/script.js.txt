// Minified version; expand for readability
document.addEventListener('DOMContentLoaded',()=>{// Smooth Scrolling
document.querySelectorAll('a[href^="#"]').forEach(anchor=>{anchor.addEventListener('click',function(e){e.preventDefault();document.querySelector(this.getAttribute('href')).scrollIntoView({behavior:'smooth'})})}); // Form Validation & Simulation (Ethical: Simulates submission; note in comments)
const forms=[document.getElementById('contact-form'),document.getElementById('newsletter-form')];forms.forEach(form=>{form.addEventListener('submit',function(e){e.preventDefault();if(this.checkValidity()){alert('Form submitted successfully! (Demo simulation - In production, integrate with Formspree or EmailJS)');this.reset()}else{alert('Please fill all fields correctly.')}})}); // Speed Test Demo (Simple JS Simulation; for real, use Speedtest.net API or fast.com embed)
function runSpeedTestDemo(){const demo=document.getElementById('speed-test-demo');const download=document.getElementById('download-speed');const upload=document.getElementById('upload-speed');const progress=document.getElementById('progress');demo.style.display='block';let p=0;const interval=setInterval(()=>{p+=Math.random()*10;if(p>100){p=100;clearInterval(interval);download.textContent='95';upload.textContent='20';progress.value=100;alert('Test complete! Download: 95 Mbps, Upload: 20 Mbps (Simulated - Real app uses accurate API)')}else{download.textContent=Math.floor(p);upload.textContent=Math.floor(p/5);progress.value=p}},200)} // GA Consent (Ethical: Only load if accepted; placeholder ID)
let gaLoaded=false;window.acceptConsent=()=>{document.getElementById('consent-banner').style.display='none';if(!gaLoaded){gaLoaded=true;// Dynamically load GA script if needed (already in HTML, but gated)
const script=document.createElement('script');script.src='https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID';document.head.appendChild(script);}}window.declineConsent=()=>{document.getElementById('consent-banner').style.display='none';// No GA load}}; // Initial consent check (hide if localStorage has consent)
if(localStorage.getItem('gaConsent')==='accepted'){acceptConsent()}else if(localStorage.getItem('gaConsent')==='declined'){declineConsent()}});